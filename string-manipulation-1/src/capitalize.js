/* exported capitalize */
function capitalize(word) {
  return word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
}

// toUpperCase the charAt index 0,

// concatenate with

// slice (cut off all previous characters) character at index 0,
// and toLowerCase the rest
